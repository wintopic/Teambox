# 怪团建隐私说明

更新日期：2026-08-17

怪团建（Teambox）是开源的本地工具箱。当前版本不要求注册账号，不包含广告、第三方行为分析或用户画像 SDK，也不会出售个人信息。

## 屏幕识别与无障碍服务

用户主动开启“举手 +1”工具后，应用会读取当前前台应用和屏幕画面，在设备本地识别目标，并通过无障碍服务执行点击。完整屏幕帧默认只在内存中处理，不写入文件、不上传服务器。关闭工具、撤回同意、关闭无障碍服务或离开受支持的目标应用后，自动点击会停止。

如果用户主动使用模板采样功能，应用只会把用户框选的局部图片保存到应用私有目录。请勿选择包含账号、头像、聊天内容或其他个人信息的区域。

## 本地保存的数据

应用可能在私有目录保存功能开关、用户同意状态、目标配置、局部模板、任务状态和不含屏幕正文的运行诊断。诊断与已结束任务会按应用内保留策略清理。系统备份和设备迁移已被禁用。

当前版本的主要运行状态保存在应用私有目录中；如需彻底清除，请在 Android 系统应用信息中清除应用数据，或直接卸载应用。清除应用数据或卸载应用会删除应用私有目录中的数据。

## 更新检测与网络访问

触发更新检测时，应用会先请求 `api.github.com` 上本仓库的公开 Releases 元数据；访问失败时，再通过固定 HTTPS 加速端点 `gh-proxy.com` 重试。网络服务可能按其自身规则记录 IP 地址、请求时间和 `Teambox-Android-UpdateChecker` User-Agent 等常规连接信息。应用不会在请求中附加账号、广告标识符、通讯录、屏幕内容、用户文件或本地团建数据。

更新检测只接受 `github.com/wintopic/Teambox` 的官方 Release 页面和 APK 地址；加速下载地址只由应用添加固定 `https://gh-proxy.com/` 前缀生成。应用禁用 HTTP 重定向，不使用明文 HTTP，也不会自动下载、安装或主动打开链接。发现新版本后，由用户决定后续操作；安装前请核对本仓库 Release 页提供的版本与校验信息。

## 系统权限

- 无障碍服务：读取当前窗口状态并执行用户开启的自动点击。
- 屏幕捕获与前台服务：在系统授权范围内取得本地识别帧，并维持运行通知。
- 网络：检查新版本。应用不申请网络状态权限。

怪团建不会尝试绕过 Android 的权限确认、安全窗口或目标应用限制。

## 第三方服务与责任边界

腾讯视频、GitHub 和更新加速服务均由第三方运营，适用各自的隐私政策和服务条款。本项目与腾讯、腾讯视频无隶属或合作关系。用户应只处理自己有权访问的内容，并遵守目标平台规则。

隐私问题可以通过仓库 Issue 提出；如果问题可能造成数据泄露，请按 [SECURITY.md](SECURITY.md) 私下报告。
